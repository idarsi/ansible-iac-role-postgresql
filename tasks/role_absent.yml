---
###############################################################################
#
# role_absent.yml
#
# @author Arsi Atomi
#
###############################################################################

###############################################################################
# SETTING VARIABLES
###############################################################################

- name: "Setting what variables are configured"
  ansible.builtin.set_fact:
    pg_db_role: "{{ pg_role_record.name }}"
  when: pg_role_record is defined

- name: "Checking if required variables are defined"
  ansible.builtin.assert:
    that:
      - pg_db_role is defined
      - pg_instance is defined
      - pg_port is defined
      - pg_version is defined
    fail_msg: "PostgreSQL role removal is missing one or more required variables"

###############################################################################
# ENSURING USER IS ABSENT
###############################################################################

- name: "Setting variables"
  ansible.builtin.set_fact:
    pg_cli_command: >-
      /usr/pgsql-{{ pg_version }}/bin/psql postgres -p {{ pg_port }}
      -tXAc "SELECT 1 FROM pg_roles WHERE rolname='{{ pg_db_role | string | replace("'", "''") }}'"

- name: "Checking if user already exists"
  become: true
  become_user: "{{ pg_os_user }}"
  ansible.builtin.command: "{{ pg_cli_command }}"
  check_mode: false
  changed_when: false
  register: role_exists

- name: "Assembling DROP ROLE psql command"
  ansible.builtin.set_fact:
    pg_cli_command: >-
      /usr/pgsql-{{ pg_version }}/bin/psql postgres -p {{ pg_port }}
    pg_cli_sql: >-
      DROP ROLE "{{ pg_db_role | string | replace('"', '""') }}"

- name: "Ensuring role is absent"
  become: true
  become_user: "{{ pg_os_user }}"
  ansible.builtin.command:
    cmd: "{{ pg_cli_command }}"
    stdin: "{{ pg_cli_sql }}"
  register: role_changed
  changed_when: "role_changed.rc == 0"
  no_log: true
  when:
    - role_exists.stdout == '1'

- name: "Writing log"
  ansible.builtin.include_tasks: log_write.yml
  vars:
    iac_log_write: "Dropped role {{ pg_version }}/{{ pg_instance }}/{{ pg_db_role }}"
